architectury {
    common(rootProject.enabled_platforms.split(","))
}
loom{
    accessWidenerPath = file("src/main/resources/mod.accesswidener")
}


dependencies {
    // @Environment annotation and mixin extras
    modImplementation(libs.fabric.loader)

    modApi(libs.architectury.lib)
    api("com.alphacephei:vosk:0.3.45")

    // ---- LOCAL DEPENDENCY
    //modApi name: 'Visor-0.3.0-snapshot#7-api_fabric'
    // ----

    // ---- EXTERNAL DEPENDENCY
    modApi(libs.visorApi)
    // ----
}

